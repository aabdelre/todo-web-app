apiVersion: v1
kind: Service
metadata:
  labels:
    service: todo-app-api
  name: todo-app-api
spec:
  type: LoadBalancer
  ports:
    - name: "http-port"
      port: 3000
      targetPort: 3000
  selector:
    service: todo-app-api

